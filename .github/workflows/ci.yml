on:
  pull_request:
    paths: hubx.sh

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pkgxdev/setup@v2
      - run: ./hubx.sh pkgxdev/demo
      - run: ./hubx.sh pkgxdev/demo   # check cache route works too
